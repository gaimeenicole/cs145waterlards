<h1>All Uploads</h1>
<div id = "annual"></div>
<div>
    <%= @sensors.each do |s| %>
        Location No: <%= s.locationNo %>
        Sequence no: <%= s.sequenceNo %>
        Sensor 1 FlowRate: <%= s.s1Flowrate %>
        Sensor 1 ML reading: <%= s.s1ML %>
        Sensor 2 FlowRate: <%= s.s2Flowrate %>
        Sensor 2 ML reading: <%= s.s2ML %>
        Delta (flow rate): <%= (s.s1Flowrate - s.s2Flowrate).abs %>
        <br/>
        <br/>
    <% end %>

    <div class="col-xs-6">
       <h3> Flow Rate by Sensor per IDK</h3>
       <%= bar_chart @sensors.group(:s1Flowrate).count %>
       <%= column_chart @sensors.map { |t| [t.s1Flowrate, t.s2Flowrate]} %>
   </div>

</div>
